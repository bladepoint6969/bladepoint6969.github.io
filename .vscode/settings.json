{
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": "\\.(yaml|yml)$",
                "cmd": "cat ${relativeFile} | yq > ${fileDirname}/${fileBasenameNoExt}.json"
            }
        ]
    }
}